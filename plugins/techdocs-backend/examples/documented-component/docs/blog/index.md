# Blog

Posts here exist to exercise the MkDocs blog plugin, which renders a second,
nested navigation sidebar inside the content column on post pages. That nested
sidebar is easy to break when changing the TechDocs reader layout.
