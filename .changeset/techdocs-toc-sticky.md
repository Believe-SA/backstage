---
'@backstage/plugin-techdocs': patch
---

Fix TechDocs reader layout so the table of contents sits beside the documentation column on wide screens, using sticky sidebars instead of viewport-fixed positioning.
